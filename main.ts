import { cardModel, messageModel } from './object-card'

export const notifyCard:Function = (error:Object) => {
  
}

export const notifyText:Function = (error:Object) => {

  let bodyMessage = messageModel(error)

  

}